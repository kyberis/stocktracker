#pragma once

/**
 * Optional one-time STA Wi‑Fi bootstrap (stored in NVS on first boot if no SSID saved).
 *
 * 1. Copy this file to `wifi_preset_secret.h` in the same folder.
 * 2. Uncomment and set SSID/pass (home networks only — do not commit the copy).
 *
 * wifi_preset_secret.h is listed in `.gitignore`.
 */
// #define TRE_WIFI_PRESET_SSID "YourSSID"
// #define TRE_WIFI_PRESET_PASS "YourPassword"
