# ----- trefolio-accounts (IdP) ------------------------------------------------
# After the IdP cutover, Will delegates auth to user.trefolio.com.
# See knowledge/design-docs/will-idp-integration.md (in trefolio repo).

IDP_BASE_URL=https://user.trefolio.com
IDP_CLIENT_ID=will
IDP_CLIENT_SECRET=
IDP_SERVICE_TOKEN=

# Feature flag during the migration window. Default true; set "false" after
# every existing Will user has been backfilled into the IdP.
USE_LEGACY_AUTH=true
