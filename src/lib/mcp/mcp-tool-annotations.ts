/** MCP tool annotations required by the Claude Connectors Directory review. */
export const MCP_READ_ONLY = { readOnlyHint: true as const };
export const MCP_WRITE = { destructiveHint: false as const };
