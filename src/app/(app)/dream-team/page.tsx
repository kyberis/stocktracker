import { DreamTeamRoom } from "@/components/dream-team/DreamTeamRoom";

export const metadata = {
  title: "Dream Team — trefolio",
  description: "Talk to Warren, Clara, and Will — your AI team",
};

export default function DreamTeamPage() {
  return <DreamTeamRoom />;
}
